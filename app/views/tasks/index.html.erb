<p>ログイン完了ユーザ: <%= current_user.name %></p>
<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <%= link_to(task.id, task) %><li><%= task.status %> &gt; <%= task.content %></li>
</ul>
<% end %>
<%= link_to '新規タスクの投稿', new_task_path %>

